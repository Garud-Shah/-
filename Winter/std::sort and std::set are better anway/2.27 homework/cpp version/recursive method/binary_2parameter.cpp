#include <iostream>
#include <vector>
#include <iterator>
#include <algorithm>
using namespace std;
vector<int> tosearch;
int element;
// this returns iterators!!!
vector<int>::iterator search(vector<int>::iterator begin, vector<int>::iterator end, vector<int>::iterator truebegin){
    if (begin == end){
        return tosearch.end();
    } else {
        vector<int>::iterator mid = 
    }
}